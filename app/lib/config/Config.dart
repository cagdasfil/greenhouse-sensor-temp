class Config {
  static const String RECIPIENT_NAME = "Birlik Mantar";
  static int PAGE_COUNT = 3;
  static int SENSOR_PER_PAGE_COUNT = 12;
  static const int SENSOR_PER_ROW_COUNT = 3;
  // Limits can be changed in settings screen
  static int UPPER_LIMIT = 20;
  static int LOWER_LIMIT = 0;
}
